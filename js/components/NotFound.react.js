const React = require('react');

const NotFound = React.createClass({
  render: function() {
    return (
      <div>
        <p>Page not found</p>
      </div>
    );
  }
});

module.exports = NotFound;

